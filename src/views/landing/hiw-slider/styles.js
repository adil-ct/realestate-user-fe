import { createStyles, makeStyles } from '@mui/styles';

const styles = makeStyles(() => createStyles({
    root: {
        padding: '20px',
    },
    container: {
        height: '375px',
        maxWidth: '680px',
        width: '100%',
    },
},
{ name : 'landing-hiw-slider' }));

export default styles;
